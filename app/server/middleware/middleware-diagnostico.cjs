// =========================================================
// diagnosticoRoutes — DISATTIVATO IN PRODUZIONE
// =========================================================

module.exports = function diagnosticoRoutes(app) {
  console.log("🟧 diagnostica-routes DISATTIVATA (SAFE MODE)");
};
