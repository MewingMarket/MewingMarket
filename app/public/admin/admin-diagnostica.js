// ==========================================
// ADMIN DIAGNOSTICA — SAFE MODE
// Versione 2027.60-SM
// Nessuna diagnostica attiva, nessun fetch, nessun test API.
// Mantiene solo un log di presenza per debug non invasivo.
// ==========================================

(function () {

  console.log("🟧 [ADMIN-DIAG] Diagnostica admin DISATTIVATA (SAFE MODE)");

  // Tutte le verifiche, fetch e scanner sono rimossi.
  // Nessuna chiamata a /api, nessun test, nessun controllo SW.
  // File completamente passivo.

})();
